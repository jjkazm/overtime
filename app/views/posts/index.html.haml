%h1 Posts
%table.table
  %thead
    %tr
      %th #
      %th Date
      %th User
      %th Rationale
  %tbody
    - @posts.each do |post|
      %tr
        %td
          = post.id
        %td
          = post.date
        %td
          = post.user.email
        %td
          = truncate(post.rationale, length: 10)
